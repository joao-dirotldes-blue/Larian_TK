# Backend environment variables (copy to .env and adjust as needed)
# This file is used by docker-compose (env_file: .env) to inject variables
# into the backend container running api.js.

# Larian gateway configuration (as used today)
LARIAN_BASE=http://34.72.225.221:8000/api/v1
LARIAN_EMAIL=seller_user@larian.local
LARIAN_PASSWORD=seller123

# Backend listening port inside the container (nginx proxies /api to this)
PORT=5174
